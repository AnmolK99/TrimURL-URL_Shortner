<!DOCTYPE html>
<html lang="en">
<head>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f3f4f6; /* Soft off-white/gray background */
      color: #333; /* Dark gray text */
      margin: 0;
      padding: 40px; 
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    h1, h3 {
      color: #2c5282; /* Soft blue for headers */
      margin-bottom: 20px;
      font-weight: 600;
      text-align: center;
    }

    h1 {
      font-size: 2.5rem;
      border-bottom: 3px solid #63b3ed; /* Blue underline */
      padding-bottom: 10px;
      margin-bottom: 30px;
    }

    form {
      width: 100%;
      max-width: 400px;
      background-color: white;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(45, 112, 190, 0.2); /* Soft blue shadow */
      border: 2px solid #63b3ed; /* Light blue border */
      display: flex;
      flex-direction: column;
    }

    label, input {
      margin-bottom: 15px;
    }

    button {
      background-color: #63b3ed;
      color: white;
      border: none;
      padding: 10px;
      border-radius: 5px;
      font-size: 1rem;
      cursor: pointer;
      margin-bottom: 10px;
    }

    button:hover {
      background-color: #4299e1;
    }

    .signup-btn {
      background-color: #48bb78; /* Green color for the signup button */
    }

    .signup-btn:hover {
      background-color: #38a169;
    }
  </style>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
</head>
<body>
  <h1>Login</h1>
  <form action="/user/login" method="post">
    <label>Email</label>
    <input type="text" required name="email"/>
    <label>Password</label>
    <input type="password" required name="password"/>

    <button type="submit">Login</button>
    
    <!-- Signup button -->
    <button type="button" class="signup-btn" onclick="window.location.href='/signup'">Sign Up</button>

    <% if (typeof loginAttempted !== 'undefined' && loginAttempted && loginSuccess) { %>
      <div style="max-width: 600px; width: 100%; margin: 20px auto; padding: 15px; background-color: #e6fffa; border: 2px solid #38b2ac; border-radius: 8px; color: #2c7a7b; text-align: center;">
        <h2 style="margin: 0; font-size: 1.5rem; font-weight: 600;">Login Successful!</h2> 
        <p style="font-size: 1rem; margin-top: 10px;">
          Welcome back! You have successfully logged in. You can now access your dashboard and manage your account.
        </p>
      </div>
    <% } else if (typeof loginAttempted !== 'undefined' && loginAttempted && !loginSuccess) { %>
      <div style="max-width: 600px; width: 100%; margin: 20px auto; padding: 15px; background-color: #f5f5f5; border: 2px solid hsl(0, 100%, 36%); border-radius: 8px; color: hsl(0, 100%, 36%); text-align: center;">
        <h2 style="margin: 0; font-size: 1.5rem; font-weight: 600;">Login Failed!</h2>
        <p style="font-size: 1rem; margin-top: 10px;">
          <%= loginMessage %>
        </p>
      </div>
    <% } %>
  </form>
</body>
</html>
