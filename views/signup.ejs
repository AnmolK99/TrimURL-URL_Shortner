<!DOCTYPE html>
<html lang="en">
<head>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f3f4f6; /* Soft off-white/gray background */
      color: #333; /* Dark gray text */
      margin: 0;
      padding: 40px; 
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    h1 {
      color: #2c5282; /* Soft blue for headers */
      font-size: 2.5rem;
      border-bottom: 3px solid #63b3ed; /* Blue underline */
      padding-bottom: 10px;
      margin-bottom: 30px;
      font-weight: 600;
      text-align: center;
    }

    form {
      width: 100%;
      max-width: 400px;
      background-color: white;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(45, 112, 190, 0.2); /* Soft blue shadow */
      border: 2px solid #63b3ed; /* Light blue border */
      display: flex;
      flex-direction: column;
    }

    label, input {
      margin-bottom: 15px;
    }

    input {
      padding: 10px;
      font-size: 1rem;
      border: 1px solid #cbd5e0; /* Light gray border */
      border-radius: 4px;
      width: 100%;
      box-sizing: border-box; /* Ensures padding is included in width */
      margin-right: 10px; /* Adds some space to the right of the input */
    }

    button {
      background-color: #63b3ed;
      color: white;
      border: none;
      padding: 10px;
      border-radius: 5px;
      font-size: 1rem;
      cursor: pointer;
      margin-top: 15px;
    }

    button:hover {
      background-color: #4299e1;
    }
  </style>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Signup</title>
</head>
<body>
  <h1>Signup</h1>
  <form action="/user/signup" method="post">
    <label>Full Name</label>
    <input type="text" required name="name"/>
    <label>Email Id</label>
    <input type="email" required name="email"/>
    <label>Password</label>
    <input type="password" required name="password"/>

    <button type="submit">Signup</button>
  </form>
</body>
</html>
